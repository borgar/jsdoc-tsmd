/**
 * Assign the project to an employee.
 *
 * @param employee The employee who is responsible for the project.
 * @param employee.department The employee's department.
 * @param employee.name The name of the employee.
 */
export declare function assignProject2(employee: {
    /** The employee's department. */
    department: string;
    /** The name of the employee. */
    name: string;
}): void;
