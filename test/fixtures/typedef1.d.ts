/** Types used to identify operations. */
export declare const operationTypes: Readonly<{
    /** A binary operation (`a+b`) */
    BINARY: string;
    /** A ternary operation (`a?b:c`) */
    TERNARY: string;
    /** A unary operation (`a%`) */
    UNARY: string;
}>;
