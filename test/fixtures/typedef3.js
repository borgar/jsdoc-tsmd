/**
 * A state collection for a user.
 * @typedef {object<string, any>} UserSettings
 * @property {boolean} isVerified
 * @property {string} email User's email.
 * @property {string=} thumbnail A URL to an image.
 */
