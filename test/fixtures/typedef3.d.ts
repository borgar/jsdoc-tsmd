/** A state collection for a user. */
export declare type UserSettings = {
    /** User's email. */
    email: string;
    isVerified: boolean;
    /** A URL to an image. */
    thumbnail?: string;
};
